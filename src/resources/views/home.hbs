<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <title>Example App - Home</title>
</head>

<body>
    <div class="container mt-4">
        <div class="row">
            {{#each courses}}
            <div class="col-sm-6 col-lg-4 mb-4 d-flex">
                <div class="card card-course-item h-100 w-100">
                    <a href="/courses/{{this.slug}}">
                        <img src="{{this.image}}" class="card-img-top" alt="{{this.name}}"style="min-height: 200px; object-fit: cover;">
                    </a>
                    <div class="card-body d-flex flex-column">
                        <a href="/courses/{{this.slug}}">

                            <h5 class="card-title">{{this.name}}</h5>
                        </a>
                        <p class="card-text">{{this.description}}</p>
                        <a href="#" class="btn btn-primary mt-auto">Mua khóa học</a>
                    </div>
                </div>
            </div>
            {{/each}}
        </div>
    </div>


</body>

</html>